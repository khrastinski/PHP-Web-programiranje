<?php
$naslov      = "PHP dokument — vježba 1c";
$autor       = "Karlo Hrastinski";
$opis        = "Ova stranica nastavlja vježbu 1b i služi za uvježbavanje varijabli, ispisa i osnovnog CSS-a.";
$linkInfo    = "https://www.php.net";
$linkNatrag  = "vjezba1b.php";
?>

<!DOCTYPE html>
<html lang="hr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>PHP dokument — vježba 1c</title>

    <style>
        :root {
            --bg: #0b1324;        /* tamna pozadina */
            --card: #ffffff;      /* bijela kartica */
            --text: #111827;      /* tamni tekst */
            --muted: #6b7280;     /* sivi sekundarni tekst */
            --accent: #2563eb;    /* plava naglasna boja */
        }

        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: var(--bg);
            color: var(--text);
            font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
            font-size: 16px;
        }

        .wrap {
            max-width: 720px;
            margin: 48px auto;
            background: var(--card);
            padding: 32px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, .10);
        }

        h1 {
            margin: 0 0 16px;
            font-size: 2rem;
            font-weight: 700;
        }

        p {
            margin: 0 0 16px;
            line-height: 1.6;
        }

        .footer {
            margin-top: 24px;
            font-size: 0.9rem;
            color: var(--muted);
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        /* Primarni gumbi */
        .btn {
            display: inline-block;
            padding: 10px 18px;
            margin-right: 8px;
            border: 1px solid var(--accent);
            border-radius: 10px;
            color: var(--accent);
            text-decoration: none;
            font-weight: 500;
            transition: background-color .18s ease, color .18s ease;
        }

        .btn:hover {
            background: var(--accent);
            color: #ffffff;
        }

        .btn:focus-visible {
            outline: 3px solid var(--accent);
            outline-offset: 2px;
        }
    </style>
</head>
<body>

    <div class="wrap">

        <h1><?php echo $naslov; ?></h1>

        <p>Ovu stranicu izradio/la je <strong><?php echo $autor; ?></strong>.</p>

        <p><?php echo $opis; ?></p>

        <a class="btn" href="<?php echo $linkInfo; ?>" target="_blank" rel="noreferrer">
            Saznaj više o PHP-u
        </a>

        <a class="btn" href="<?php echo $linkNatrag; ?>">
            Natrag na vježba 1b
        </a>

        <p class="footer">
            © <?php echo date('Y'); ?> — Demo za PHP
        </p>
    </div>

    <!-- Naziv datoteke: vjezba1c.php -->
</body>
</html>
